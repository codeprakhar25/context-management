# Core: note store, retrieval baselines, and every API-based placer.
openai>=1.40.0
numpy>=1.26.0
python-dotenv>=1.0.0

# Local LoRA training only (modal_sft_placer.py). Skip unless you are
# retraining the adapter; the evaluation scripts do not import these.
# modal>=0.64
# torch>=2.3
# transformers>=4.44
# peft>=0.12
# datasets>=2.20
